# لا توجد أي أسرار حقيقية هنا. هذا الملف Placeholder فقط لتوضيح شكل
# الإعدادات المستقبلية عند إضافة Backend/Secure Proxy حقيقي.
#
# النسخة الحالية (Static Web App / Mock Engine) لا تحتاج أي متغير بيئة
# لتعمل — تُخدَّم كملفات ثابتة فقط.

# مستقبلاً: عنوان الـBackend الآمن الذي سيستدعي مزودي Threat Intelligence
# نيابة عن الواجهة الأمامية (لا يوضع أي API Key هنا أو في أي كود Frontend)
# VITE_OR_APP_BACKEND_BASE_URL=https://api.example.com

# مستقبلاً: تفعيل وضع Live بدلاً من Development في js/core/config.js
# APP_ANALYSIS_MODE=development
